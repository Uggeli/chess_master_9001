defaults:
  - base

data:
  train_path: data/processed/train.npz
  val_path: data/processed/val.npz

training:
  loss_weights:
    policy: 1.0
    value: 1.0
    retrieval: 0.1
    opponent: 0.5

active_phase: 2
run_dir: runs/phase_b
